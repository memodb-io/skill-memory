---
name: skill-memory
description: Use skill-memory to download, view and edit agent skills in your memory. TRIGGER by (download skill to memory, view skill in memory, edit skill in memory)
---

## Prerequisites
- `skill-memory` cli: `npm install -g skill-memory`


## Setup
- read `./recipe.md` to finish `skill-memory` setup.